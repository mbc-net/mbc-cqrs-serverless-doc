{
  "latest": {
    "version": "1.3.4",
    "date": "2026-07-17",
    "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.3.4",
    "features": [],
    "bugFixes": [
      "Lower the default `ATTRIBUTE_LIMIT_SIZE` from `389120` (380 KB) to `102400` (100 KB) in CDK infra templates and env examples — the previous default was sized for DynamoDB's 400 KB item limit, but Step Functions enforces a 256 KB payload limit per state, and the command state machine passes the DynamoDB stream event twice (`input.$` + `context.$`), so inline attributes above ~110 KB could trigger `States.DataLimitExceeded`",
      "Update skills with guidance on the new `ATTRIBUTE_LIMIT_SIZE` default"
    ],
    "security": []
  },
  "recent": [
    {
      "version": "1.3.4",
      "date": "2026-07-17",
      "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.3.4",
      "features": [],
      "bugFixes": [
        "Lower the default `ATTRIBUTE_LIMIT_SIZE` from `389120` (380 KB) to `102400` (100 KB) in CDK infra templates and env examples — the previous default was sized for DynamoDB's 400 KB item limit, but Step Functions enforces a 256 KB payload limit per state, and the command state machine passes the DynamoDB stream event twice (`input.$` + `context.$`), so inline attributes above ~110 KB could trigger `States.DataLimitExceeded`",
        "Update skills with guidance on the new `ATTRIBUTE_LIMIT_SIZE` default"
      ],
      "security": []
    },
    {
      "version": "1.3.2",
      "date": "2026-06-27",
      "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.3.2",
      "features": [
        "Add AP028, AP029, AP030 anti-pattern detectors"
      ],
      "bugFixes": [
        "Fix `DataSyncHandler` duplicate registration — `ExplorerService` now deduplicates handlers via `Set`; `CommandService` deduplicates via `Map<constructor.name, instance>` and emits a `warn` log for each removed duplicate",
        "Fix `publishSync` — versioned `sk` is now assigned to `command.sk` before handler dispatch so `IDataSyncHandler.up()` receives the correct key; `updateStatus` errors are no longer silently masked; `pk`/`sk` are now included in `checkNextToken` warn logs"
      ],
      "security": [
        "Harden npm dependencies — resolve high/moderate vulnerabilities via `overrides` (`lodash`, `tmp`, `multer`, `ajv`, `picomatch`, `fast-xml-parser`) and targeted upgrades (`@nestjs/config` ^4.0.4, `@typescript-eslint` v8); root production audit: 0 critical/high",
        "Add blocking CI gates: `npm audit --omit=dev --audit-level=high` and ESLint startup check run on every PR, failing the build on any production vulnerability"
      ]
    },
    {
      "version": "1.3.1",
      "date": "2026-06-02",
      "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.3.1",
      "features": [
        "Add group-based role authorization — `RolesGuard` checks direct roles from `custom:roles` first, then roles derived from the user's groups in the new `custom:groups` claim",
        "Update skills and analyzer for v1.3.1"
      ],
      "bugFixes": [
        "Tolerate a malformed `custom:groups` claim (fail-closed to no group roles) instead of throwing, so a bad token degrades to direct-role-only checks rather than crashing the request"
      ],
      "security": []
    },
    {
      "version": "1.3.0",
      "date": "2026-05-21",
      "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.3.0",
      "features": [
        "Add `AppSyncEventsService` — opt-in HTTP pub/sub transport for real-time notifications via AWS AppSync Events API",
        "CDK infra template: add `appsyncEvents` and `notificationTransports` to `Config` type — provisions `EventApi` + `ChannelNamespace`, injects `NOTIFICATION_TRANSPORTS`, `APPSYNC_EVENTS_ENDPOINT`, and `APPSYNC_EVENTS_NAMESPACE` into Lambda and ECS, and grants `appsync:EventPublish` IAM permission via `grantPublish()` automatically",
        "Update skills and analyzer for v1.3.0"
      ],
      "bugFixes": [],
      "security": []
    }
  ],
  "generated": "2026-07-17T07:48:33.411Z"
}