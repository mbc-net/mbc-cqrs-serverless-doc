{
  "latest": {
    "version": "1.2.6",
    "date": "2026-05-06",
    "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.2.6",
    "features": [
      "Repository Read-Your-Writes (RYW) improvements — proactively purge stale RYW sessions when the data table catches up to or surpasses the session version, eliminating \"stale override\" reads",
      "Add AP021 anti-pattern detector — `Event Emit Directly After publishAsync in CommandService` (Severity: High)",
      "Add cross-reference table mapping detector AP codes (`analyze.ts`) to skill-doc AP codes (`skills/mbc-review/SKILL.md`) — the two systems use independent numbering and only AP016–AP019 and AP021 share codes"
    ],
    "bugFixes": [
      "`server.ts` was hardcoding the package version, which silently drifted on every `lerna version` bump. Now read at runtime from `package.json`"
    ],
    "security": []
  },
  "recent": [
    {
      "version": "1.2.6",
      "date": "2026-05-06",
      "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.2.6",
      "features": [
        "Repository Read-Your-Writes (RYW) improvements — proactively purge stale RYW sessions when the data table catches up to or surpasses the session version, eliminating \"stale override\" reads",
        "Add AP021 anti-pattern detector — `Event Emit Directly After publishAsync in CommandService` (Severity: High)",
        "Add cross-reference table mapping detector AP codes (`analyze.ts`) to skill-doc AP codes (`skills/mbc-review/SKILL.md`) — the two systems use independent numbering and only AP016–AP019 and AP021 share codes"
      ],
      "bugFixes": [
        "`server.ts` was hardcoding the package version, which silently drifted on every `lerna version` bump. Now read at runtime from `package.json`"
      ],
      "security": []
    },
    {
      "version": "1.2.5",
      "date": "2026-04-10",
      "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.2.5",
      "features": [
        "Refactor ZIP import processing — move ZIP job handling directly into `ImportService`, removing the separate `ZipImportQueueEventHandler`",
        "Add AP016–AP020 anti-pattern detectors to `mbc_check_anti_patterns` tool (total: 20 patterns)",
        "Update `mbc-migrate` and `mbc-debug` skills with v1.1.x and v1.2.x breaking change guidance"
      ],
      "bugFixes": [],
      "security": []
    },
    {
      "version": "1.2.2",
      "date": "2026-04-08",
      "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.2.2",
      "features": [],
      "bugFixes": [
        "Fix Head-of-Line Blocking (Poison Pill) in `CsvBatchProcessor` by implementing Smart Retry pattern",
        "Fix `ImportQueueEventHandler` passing raw SQS payload instead of parsed `importEvent` to `SingleImportProcessor`"
      ],
      "security": []
    },
    {
      "version": "1.2.1",
      "date": "2026-04-06",
      "url": "https://github.com/mbc-net/mbc-cqrs-serverless/releases/tag/v1.2.1",
      "features": [
        "Add `SqsService` and `SqsClientFactory` for SQS message operations",
        "Refactor `SnsClientFactory` to use a singleton `SNSClient` instance"
      ],
      "bugFixes": [],
      "security": []
    }
  ],
  "generated": "2026-05-06T06:00:31.004Z"
}