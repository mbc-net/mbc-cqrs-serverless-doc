{
  "Learn how to use DataService for querying data from DynamoDB tables.": "Learn how to use DataService for querying data from DynamoDB tables.",
  "DataService": "DataService",
  "Overview": "Overview",
  "The `DataService` is the query side of the CQRS pattern, providing efficient read operations for data stored in DynamoDB. It handles all read operations from the data table (the read model) which is optimized for queries.": "The `DataService` is the query side of the CQRS pattern, providing efficient read operations for data stored in DynamoDB. It handles all read operations from the data table (the read model) which is optimized for queries.",
  "Read-Your-Writes Consistency": "Read-Your-Writes Consistency",
  "`DataService` reads from the DynamoDB data table, which is updated asynchronously via DynamoDB Streams. If you need a user to immediately see their own writes (before the stream propagates), use `Repository` instead — it is a drop-in replacement for `DataService` that adds RYW session caching. See [Read-Your-Writes in CommandService](/docs/command-service#read-your-writes) for setup and usage.": "`DataService` reads from the DynamoDB data table, which is updated asynchronously via DynamoDB Streams. If you need a user to immediately see their own writes (before the stream propagates), use `Repository` instead — it is a drop-in replacement for `DataService` that adds RYW session caching. See [Read-Your-Writes in CommandService](/docs/command-service#read-your-writes) for setup and usage.",
  "CQRS Query Side": "CQRS Query Side",
  "Application": "Application",
  "Data Table": "Data Table",
  "Query Results": "Query Results",
  "Before using the DataService, you need to set up the CommandModule as described in [the CommandService section](/docs/command-service).": "Before using the DataService, you need to set up the CommandModule as described in [the CommandService section](/docs/command-service).",
  "Methods": "Methods",
  "*async* `getItem(key: DetailKey): Promise<DataModel | undefined>`": "*async* `getItem(key: DetailKey): Promise<DataModel | undefined>`",
  "The `getItem` method returns a set of attributes for the item with the given detail/primary key. If there is no matching item, `getItem` returns `undefined`.": "The `getItem` method returns a set of attributes for the item with the given detail/primary key. If there is no matching item, `getItem` returns `undefined`.",
  "Example:": "Example:",
  "*async* `listItemsByPk(pk: string, opts?: ListItemsOptions): Promise<DataListEntity>`": "*async* `listItemsByPk(pk: string, opts?: ListItemsOptions): Promise<DataListEntity>`",
  "The `listItemsByPk` method returns one or more items matching the partition key. It supports filtering, pagination, and sorting.": "The `listItemsByPk` method returns one or more items matching the partition key. It supports filtering, pagination, and sorting.",
  "Basic Usage": "Basic Usage",
  "List all items by primary key (`pk`):": "List all items by primary key (`pk`):",
  "With Sort Key Filter": "With Sort Key Filter",
  "List items by primary key (`pk`) and use a filter expression on the sort key (`sk`). For example, get items where the sort key starts with `CAT#` and limit to 100 items:": "List items by primary key (`pk`) and use a filter expression on the sort key (`sk`). For example, get items where the sort key starts with `CAT#` and limit to 100 items:",
  "Pagination": "Pagination",
  "Implement pagination using `startFromSk` and `limit`:": "Implement pagination using `startFromSk` and `limit`:",
  "Sort Key Operators": "Sort Key Operators",
  "The following sort key expressions are supported:": "The following sort key expressions are supported:",
  "Operator": "Operator",
  "Expression": "Expression",
  "Description": "Description",
  "Equals": "Equals",
  "Exact match": "Exact match",
  "Begins With": "Begins With",
  "Prefix match": "Prefix match",
  "Between": "Between",
  "Range query": "Range query",
  "Less Than": "Less Than",
  "Less than comparison": "Less than comparison",
  "Greater Than": "Greater Than",
  "Greater than comparison": "Greater than comparison",
  "Example with range query:": "Example with range query:",
  "*async* `publish(cmd: CommandModel): Promise<DataModel>`": "*async* `publish(cmd: CommandModel): Promise<DataModel>`",
  "The `publish` method publishes command data to the data table. This is typically called internally by the framework's data sync handlers, but can be used directly when implementing custom synchronization logic.": "The `publish` method publishes command data to the data table. This is typically called internally by the framework's data sync handlers, but can be used directly when implementing custom synchronization logic.",
  "This method is primarily used internally by the framework. In most cases, you should use `CommandService.publishSync()` or `CommandService.publishAsync()` which automatically handles data synchronization.": "This method is primarily used internally by the framework. In most cases, you should use `CommandService.publishSync()` or `CommandService.publishAsync()` which automatically handles data synchronization.",
  "Custom data sync handler example": "Custom data sync handler example",
  "Publish command to data table": "Publish command to data table",
  "Additional synchronization to external systems": "Additional synchronization to external systems",
  "Handle rollback if needed": "Handle rollback if needed",
  "Custom sync logic": "Custom sync logic",
  "The method:": "The method:",
  "Converts CommandModel to DataModel format": "Converts CommandModel to DataModel format",
  "Removes version suffix from sort key": "Removes version suffix from sort key",
  "Preserves original creation metadata (createdAt, createdBy, createdIp)": "Preserves original creation metadata (createdAt, createdBy, createdIp)",
  "Updates modification metadata (updatedAt, updatedBy, updatedIp)": "Updates modification metadata (updatedAt, updatedBy, updatedIp)",
  "Stores the data in the data table": "Stores the data in the data table",
  "Common Patterns": "Common Patterns",
  "Search by Code": "Search by Code",
  "Find an item by its unique code within a tenant:": "Find an item by its unique code within a tenant:",
  "List with Type Filter": "List with Type Filter",
  "List items filtered by type:": "List items filtered by type:",
  "Error Handling": "Error Handling",
  "Handle common query errors gracefully:": "Handle common query errors gracefully:",
  "Log and rethrow unexpected errors": "Log and rethrow unexpected errors",
  "Type Definitions": "Type Definitions",
  "DetailKey": "DetailKey",
  "ListItemsOptions": "ListItemsOptions",
  "`ListItemsOptions` is an inline type definition in the method signature, not a separately exported interface. The type structure is as follows:": "`ListItemsOptions` is an inline type definition in the method signature, not a separately exported interface. The type structure is as follows:",
  "Default: 10": "Default: 10",
  "DataListEntity": "DataListEntity",
  "`DataListEntity` is a class (not an interface) that wraps list query results. It provides a constructor for easy instantiation:": "`DataListEntity` is a class (not an interface) that wraps list query results. It provides a constructor for easy instantiation:",
  "Array of data entities": "Array of data entities",
  "Sort key for pagination cursor": "Sort key for pagination cursor",
  "Total count (if available)": "Total count (if available)",
  "The constructor accepts a partial object, allowing you to create instances from query results:": "The constructor accepts a partial object, allowing you to create instances from query results:",
  "Best Practices": "Best Practices",
  "Use projection expressions": "Use projection expressions",
  "Only retrieve the attributes you need to reduce data transfer": "Only retrieve the attributes you need to reduce data transfer",
  "Implement pagination": "Implement pagination",
  "Always paginate large result sets to avoid memory issues": "Always paginate large result sets to avoid memory issues",
  "Cache frequently accessed data": "Cache frequently accessed data",
  "Consider caching static or slowly changing data": "Consider caching static or slowly changing data",
  "Use appropriate key design": "Use appropriate key design",
  "Design your keys to support your query patterns efficiently": "Design your keys to support your query patterns efficiently",
  "Handle not found cases": "Handle not found cases",
  "Always check if the item exists before using it": "Always check if the item exists before using it",
  "Related Documentation": "Related Documentation",
  "Command Service": "Command Service",
  "Writing commands with CommandService": "Writing commands with CommandService",
  "Service Patterns": "Service Patterns",
  "Complete service layer patterns": "Complete service layer patterns",
  "Key Patterns": "Key Patterns",
  "DynamoDB key design for efficient queries": "DynamoDB key design for efficient queries",
  "Entity Patterns": "Entity Patterns",
  "Entity definition with DataEntity": "Entity definition with DataEntity",
  "HistoryService": "HistoryService",
  "The `HistoryService` provides access to the history table, which stores all previous versions of commands. Use it when you need to retrieve a specific historical version of an entity for audit trails or rollback scenarios.": "The `HistoryService` provides access to the history table, which stores all previous versions of commands. Use it when you need to retrieve a specific historical version of an entity for audit trails or rollback scenarios.",
  "Retrieves a specific versioned item from the history table. The sort key must include the version suffix (use `addSortKeyVersion` to build it). Returns `undefined` if the version does not exist.": "Retrieves a specific versioned item from the history table. The sort key must include the version suffix (use `addSortKeyVersion` to build it). Returns `undefined` if the version does not exist.",
  "The history table uses the same key structure as the data table, but the sort key includes the version suffix: `sk@version`. Always use `addSortKeyVersion(sk, version)` to construct the key.": "The history table uses the same key structure as the data table, but the sort key includes the version suffix: `sk@version`. Always use `addSortKeyVersion(sk, version)` to construct the key.",
  "Partition key": "Partition key",
  "Sort key": "Sort key"
}